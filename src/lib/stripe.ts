// Stripe integration placeholder — not active for this store.
// Orders are submitted via the checkout form and handled manually.
export const getStripeEnabled = async () => false
export const createCheckoutSession = async (_productId: number) => null
